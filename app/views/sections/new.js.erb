$("#admin-drawer .form-block").html("<%= escape_javascript(render partial: 'sections/form', locals: { page: @page, section: @section, button_text: 'Create Section' }) %>");
$("#admin-drawer .form-title").text('Add Section');
adminPageBuilder.slideInAdminDrawer();