$("#admin-drawer .form-block").html("<%= escape_javascript(render partial: 'rows/form', locals: { row: @row, section: @section, button_text: 'Add Row' }) %>");
$("#admin-drawer .form-title").text('Add Row');
$("#admin-drawer").show();