<% upload = Upload.find(image.upload_id) %>
<% if image.url_link != '' %>
  <%= link_to image_tag(upload.myfile.url, :alt => image.caption, :title => image.caption), image.url_link %>
<% else %>
  <%= image_tag(upload.myfile.url, :alt => image.caption, :title => image.caption) %>
<% end %>